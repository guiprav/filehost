{
  "scripts": {
    "mount:public": "mount public --to /",
    "mount:src": "mount src --to /_dist_",
    "build:tsx": "esbuild --jsx-factory=d.el --loader=tsx",
    "run:tsc": "tsc --noEmit",
    "run:tsc::watch": "$1 --watch"
  },
  "plugins": []
}
